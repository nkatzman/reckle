'use strict';

module.exports = {
    comparisons: require('./comparisons'),
    dataLoader: require('./dataLoader'),
    requests: require('./requests'),
    store: require('./store'),
    users: require('./users')
};
